<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 오디오 권한 -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <!-- 인터넷 권한 -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- 파일 접근 권한-->
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO"/>
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Globa"
        tools:targetApi="34">
        <activity
            android:name=".theme.ThemeActivity"
            android:exported="false" />
        <activity
            android:name=".withdraw.WithdrawActivity"
            android:exported="false" />
        <activity
            android:name=".profile.edit.NicknameEditActivity"
            android:exported="false" />

        <activity android:name=".profile.info.MyinfoActivity"/>

        <activity
            android:name=".intro.IntroActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".login.LoginActivity"/>
        <activity android:name=".main.MainActivity"/>
        <activity android:name=".profile.service_info.ServiceInfoActivity"/>
        <activity android:name=".profile.help.HelpActivity"/>
        <activity android:name=".profile.alert.AlertActivity"/>
        <activity android:name=".profile.theme.ThemeActivity"/>
        <activity android:name=".keyword.detail.KeywordDetailActivity"/>
        <activity android:name=".folder.permission.FolderPermissionActivity"/>
        <activity
            android:name=".conditions.ConditionsActivity"
            android:exported="false">
        </activity>
    </application>

</manifest>